"""Exercise: TDD Basics.

Goal:
Create `my_solution/string_calculator.py`.

Requirements:
- `add("")` returns `0`.
- `add("1")` returns `1`.
- `add("1,2,3")` returns `6`.
- Raise `ValueError` listing negative numbers.

Recommended approach:
Work in red-green-refactor cycles instead of trying to solve everything in one pass.
"""
