"""Exercise: Pytest Basics.

Goal:
Create `my_solution/calculator.py` with a small API that can be tested with pytest.

Requirements:
- Implement `add(left, right)`.
- Implement `subtract(left, right)`.
- Implement `divide(left, right)` and raise `ValueError` for zero division.
- Implement `classify_number(value)` returning `positive`, `negative`, or `zero`.

Suggested workflow:
1. Copy this specification into `my_solution/calculator.py`.
2. Write the implementation.
3. Run the tests in `tests/test_basic.py`.
4. Add at least one extra edge case of your own.

Do not modify this file directly.
"""
